import {
  hasMeaningfulAmount,
  shouldIncludeItem,
  resolveCategoryMeta,
  summarizeItems,
  calculateMetrics,
  calculateContractMetrics,
  buildCategories,
} from "@js/domain.js";

export const domainStore = {
  hasMeaningfulAmount,
  shouldIncludeItem,
  resolveCategoryMeta,
  summarizeItems,
  calculateMetrics,
  calculateContractMetrics,
  buildCategories,
};
